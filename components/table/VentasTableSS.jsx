// componentes externos
// componentes propios
// funciones y hooks
// recurso (img, js, css)

const VentasTableSS = ({ children, className}) => {
  return (
    <tabless className={`w-50 ${className}`}>
      {children}
    </tabless>
  )
}

export default VentasTableSS
